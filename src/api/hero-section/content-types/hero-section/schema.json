{
  "kind": "singleType",
  "collectionName": "hero_sections",
  "info": {
    "singularName": "hero-section",
    "pluralName": "hero-sections",
    "displayName": "Hero Section"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "default": "Enjoy Decor",
      "required": true
    },
    "description": {
      "type": "text",
      "default": "We craft interiors that breathe personality into every corner — blending timeless elegance with modern living for spaces you'll never want to leave.",
      "required": true
    },
    "cover_img": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": [
        "images",
        "files",
        "videos"
      ]
    },
    "project_name": {
      "type": "string",
      "required": true
    }
  }
}
